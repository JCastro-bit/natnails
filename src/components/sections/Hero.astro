---
export interface Props {
  title?: string
  subtitle?: string
  ctaText?: string
  ctaLink?: string
  image?: string
}

const {
  title = 'CURSOS',
  subtitle = 'ONLINE',
  ctaText = 'Ver Cursos',
  ctaLink = 'https://natnails.cloud',
  image = '/images/natalia-natnails-hero.webp',
} = Astro.props
---

<div class="relative bg-gradient-to-br from-pink-500 to-pink-600">
  <div class="mx-auto max-w-7xl lg:grid lg:grid-cols-12 lg:gap-x-8 lg:px-8">
    <div
      class="px-6 pt-10 pb-24 sm:pb-32 lg:col-span-7 lg:px-0 lg:pt-40 lg:pb-48 xl:col-span-6"
    >
      <div class="mx-auto max-w-lg lg:mx-0">
        <h1
          class="mt-24 text-5xl font-black tracking-tight text-pretty text-white sm:mt-10 sm:text-7xl"
        >
          {title}
          <br />
          <span class="text-black">{subtitle}</span>
        </h1>

        <p
          class="mt-8 text-lg font-medium text-pretty text-white/90 sm:text-xl/8"
        >
          Transforma tu pasión por las uñas en una profesión exitosa. Aprende
          técnicas avanzadas de nail art, caricatura y diseños únicos.
        </p>

        <div class="mt-10 flex items-center gap-x-6">
          <a
            href={ctaLink}
            target="_blank"
            rel="noopener noreferrer"
            class="rounded-full bg-white px-4 py-3 text-sm font-bold text-pink-600 shadow-lg transition-all duration-300 hover:scale-105 hover:bg-gray-100"
          >
            {ctaText}
          </a>
          <a href="#cursos" class="text-sm/6 font-semibold text-white">
            Conoce más <span aria-hidden="true">→</span>
          </a>
        </div>
      </div>
    </div>

    <div
      class="relative lg:col-span-5 lg:-mr-8 xl:absolute xl:inset-0 xl:left-1/2 xl:mr-0"
    >
      <img
        src={image}
        alt="Natalia - Instructora profesional de nail art"
        class="aspect-3/2 w-full bg-pink-400 object-cover lg:absolute lg:inset-0 lg:aspect-auto lg:h-full"
      />
    </div>
  </div>
</div>
