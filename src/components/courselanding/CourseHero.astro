---

---

<section
  class="relative flex min-h-screen items-center justify-center overflow-hidden"
>
  <!-- Capa 1 - Fondo con textura de papel -->
  <div class="absolute inset-0">
    <img
      src="/images/paper-texture.webp"
      alt="Paper texture background"
      class="h-full w-full object-cover"
    />
  </div>
  <!-- Capa 2 - Contenedor principal con borde y esquinas redondeadas -->
  <div
    class="absolute top-1/2 left-1/2 z-10 flex h-[calc(100vh-6rem)] w-[92%] -translate-x-1/2 -translate-y-1/2 items-center justify-center overflow-hidden rounded-3xl border border-black sm:h-[calc(100vh-8rem)] sm:w-[88%] md:h-[calc(100vh-10rem)] md:w-[84%] lg:h-[calc(100vh-12rem)] lg:w-[80%] xl:h-[calc(100vh-14rem)] xl:w-[76%] 2xl:h-[calc(100vh-16rem)] 2xl:w-[72%]"
  >
    <!-- Imagen de fondo del contenedor -->
    <div class="absolute inset-0 rounded-3xl">
      <img
        src="/images/bg-cover.webp"
        alt="Background"
        class="h-full w-full rounded-3xl object-cover"
      />
    </div>
    <!-- Elementos decorativos internos -->
    <div class="absolute inset-0 overflow-hidden rounded-3xl">
      <div
        class="absolute -top-40 -right-40 h-80 w-80 rounded-full bg-pink-200/30 blur-xl"
      >
      </div>
      <div
        class="absolute -bottom-40 -left-40 h-80 w-80 rounded-full bg-pink-200/30 blur-xl"
      >
      </div>
    </div>
    <!-- Contenido del hero -->
    <div
      class="relative z-30 mx-auto max-w-7xl px-4 pt-20 text-center sm:px-6 lg:px-8"
    >
      <!-- Titular potente y claro sobre el beneficio -->
      <div
        class="mb-4 inline-block rounded-full bg-yellow-400 px-6 py-2 text-sm font-bold text-black md:text-base"
      >
        🔥 OFERTA LIMITADA - SOLO HOY
      </div>

      <h1
        class="mb-6 text-4xl leading-tight font-black text-pink-600 md:text-6xl lg:text-7xl"
      >
        Domina el Arte de<br />
        <span class="online-text font-light italic">Caricaturas</span><br />
        <span class="font-black text-gray-800">en Uñas</span>
      </h1>

      <p
        class="mx-auto mb-6 max-w-4xl text-xl leading-relaxed font-medium text-gray-800 md:text-2xl"
      >
        <span class="font-bold text-pink-600"
          >Transforma tu pasión en una fuente de ingresos</span
        ><br />
        Curso 100% online • Sin experiencia previa • Asesoría personalizada<br
        />
        <span class="text-lg text-gray-600"
          >✨ Aprende las técnicas que usan las profesionales más exitosas</span
        >
      </p>

      <!-- Prueba social rápida -->
      <div
        class="mb-8 flex items-center justify-center space-x-6 text-sm text-gray-600"
      >
        <div class="flex items-center">
          <span class="text-yellow-500">⭐⭐⭐⭐⭐</span>
          <span class="ml-1">4.9/5</span>
        </div>
        <div>500+ alumnas</div>
        <div>8 años de experiencia</div>
      </div>

      <!-- CTAs urgentes -->
      <div class="space-y-4">
        <a
          href="#precio"
          class="relative z-40 inline-block animate-pulse rounded-full bg-gradient-to-r from-pink-600 to-purple-600 px-10 py-5 text-lg font-bold text-white transition-all hover:scale-105 hover:shadow-2xl"
        >
          🚀 INSCRÍBETE HOY - $300 DE DESCUENTO
        </a>
        <div class="text-sm text-gray-600">
          ⏰ Quedan solo 24 horas para aprovechar esta oferta
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Estilos para el texto decorativo */
  .online-text {
    font-family: 'Corinthia', cursive;
    font-weight: 700;
    text-shadow:
      0 0 10px rgba(72, 236, 233, 0.6),
      0 0 20px rgba(248, 244, 246, 0.4),
      0 0 30px rgba(255, 255, 255, 0.3),
      0 0 40px rgba(236, 72, 153, 0.2);
    filter: drop-shadow(0 0 8px rgba(236, 72, 153, 0.8));
  }
</style>
